# Replace every instance of "s" in "A screaming comes across the sky." with a dollar sign.

sample_string = "A screaming comes across the sky."

new_string = sample_string.replace("s", "$")

print(new_string)
